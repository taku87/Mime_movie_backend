<%= form_with(model: content_video_comment, local: true) do |form| %>
  <% if content_video_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_video_comment.errors.count, "error") %> prohibited this content_video_comment from being saved:</h2>

      <ul>
      <% content_video_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
