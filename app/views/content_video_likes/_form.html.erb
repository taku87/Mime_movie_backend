<%= form_with(model: content_video_like, local: true) do |form| %>
  <% if content_video_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_video_like.errors.count, "error") %> prohibited this content_video_like from being saved:</h2>

      <ul>
      <% content_video_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
